name: Autorelease main with minor version bumps
on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  autorelease:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Autorelease Trigger
        id: autorelease
        uses: a10y/autorelease-action@552465cfb3a69751157dc800bc210c1e5ba20790
        with:
          token: "${{ secrets.GITHUB_TOKEN }}"
